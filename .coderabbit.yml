version: 1

# 기본 동작 설정
general:
  # 어떤 PR에 리뷰를 달지
  review:
    # 최소 변경 라인 수 (이 미만이면 리뷰 안함)
    min_changed_lines: 5

    # 자동 리뷰 트리거 조건
    triggers:
      on_open: true          # PR 생성 시
      on_update: true        # 커밋 추가 등 업데이트 시
      on_reopen: true        # 닫혔다가 다시 열릴 때

    # 리뷰 깊이 (low | medium | high)
    depth: medium

    # 코멘트 스타일
    comment_style:
      tone: friendly         # friendly | strict | neutral
      language: ko           # ko | en 등 (주로 코멘트 언어)
      max_comments: 30       # 너무 많은 코멘트 방지

  # 리뷰 대상 브랜치 필터
  branches:
    include:
      - main
      - develop
      - "feature/*"
      - "refactor/*"
      - "test/*"
    exclude:
      - "chore/*"

# 코드 스타일 / 품질 관련 가이드
guidelines:
  # 언어별 기본 룰
  languages:
    java:
      prefer_stream_api: true
      enforce_immutability: true
      require_null_checks: true
      avoid_raw_types: true
    yaml:
      require_valid_syntax: true

  # 공통 코드 스타일
  style:
    # 메서드 길이, 클래스 복잡도 같은 것에 대해 언급해 줘
    complexity:
      warn_on_long_methods: true
      max_method_length: 40     # 줄 수 기준
      max_parameters: 5
    naming:
      check_consistency: true

  # 테스트 관련
  tests:
    require_tests_for_new_code: true
    require_tests_for_bugs: true
    check_test_quality: true    # 의미 없는 테스트 (assert 없이 호출만 하는 등) 경고

# 코멘트 강도 설정
severity:
  levels:
    # block: 머지 전에 꼭 수정해야 할 것
    - name: error
      label: "❌ 꼭 수정 필요"
      block_merge: true

    # 권장: 가능하면 고치기
    - name: warning
      label: "⚠️ 수정 권장"
      block_merge: false

    # 그냥 팁: 개선 아이디어
    - name: info
      label: "💡 제안"
      block_merge: false


